const year=document.getElementById("year");if(year)year.textContent=new Date().getFullYear();const canvas=document.getElementById("starfield"),ctx=canvas.getContext("2d",{alpha:true});let stars=[];function resizeCanvas(){const dpr=Math.min(window.devicePixelRatio||1,2);canvas.width=Math.floor(window.innerWidth*dpr);canvas.height=Math.floor(window.innerHeight*dpr);ctx.setTransform(dpr,0,0,dpr,0,0);stars=Array.from({length:Math.min(180,Math.floor(window.innerWidth/7))},()=>({x:Math.random()*window.innerWidth,y:Math.random()*window.innerHeight,s:Math.random()>.82?2:1,v:.15+Math.random()*.45,c:Math.random()>.72?"rgba(53,242,165,.72)":"rgba(244,198,79,.44)"}))}function drawStars(){ctx.clearRect(0,0,window.innerWidth,window.innerHeight);for(const s of stars){ctx.fillStyle=s.c;ctx.fillRect(Math.floor(s.x),Math.floor(s.y),s.s,s.s);s.y+=s.v;if(s.y>window.innerHeight){s.y=-4;s.x=Math.random()*window.innerWidth}}requestAnimationFrame(drawStars)}resizeCanvas();window.addEventListener("resize",resizeCanvas);drawStars();const terminal=document.getElementById("terminal-output"),lines=["scan backlog... TECH / PRIVACY / GAMES / RPG","compile chiptune loop... READY","calibrate CRT glow... OK","sync asset forge... OK","publish target... GitHub Pages","privacy stance... no trackers, no CDN"];let lineIndex=0;setInterval(()=>{if(!terminal)return;terminal.textContent+=`\n${lines[lineIndex]}`;lineIndex=(lineIndex+1)%lines.length;const all=terminal.textContent.split("\n");if(all.length>10)terminal.textContent=all.slice(-10).join("\n")},2400);let audioCtx,playing=false,intervalId,masterGain;const btn=document.getElementById("music-toggle"),melody=[220,277.18,329.63,440,329.63,277.18,246.94,329.63,220,293.66,349.23,440,392,329.63,293.66,246.94],bass=[55,55,82.41,82.41,73.42,73.42,65.41,65.41];function beep(freq,time,duration,type="square",gain=.05){const osc=audioCtx.createOscillator(),g=audioCtx.createGain();osc.type=type;osc.frequency.setValueAtTime(freq,time);g.gain.setValueAtTime(.0001,time);g.gain.exponentialRampToValueAtTime(gain,time+.012);g.gain.exponentialRampToValueAtTime(.0001,time+duration);osc.connect(g);g.connect(masterGain);osc.start(time);osc.stop(time+duration+.02)}function hat(time){const bufferSize=Math.floor(audioCtx.sampleRate*.025),buffer=audioCtx.createBuffer(1,bufferSize,audioCtx.sampleRate),data=buffer.getChannelData(0);for(let i=0;i<bufferSize;i++)data[i]=(Math.random()*2-1)*.18;const noise=audioCtx.createBufferSource(),g=audioCtx.createGain();g.gain.setValueAtTime(.03,time);g.gain.exponentialRampToValueAtTime(.0001,time+.025);noise.buffer=buffer;noise.connect(g);g.connect(masterGain);noise.start(time);noise.stop(time+.03)}function startTune(){audioCtx=audioCtx||new(window.AudioContext||window.webkitAudioContext);masterGain=audioCtx.createGain();masterGain.gain.value=.26;masterGain.connect(audioCtx.destination);let step=0;intervalId=setInterval(()=>{const t=audioCtx.currentTime,n=melody[step%melody.length],b=bass[Math.floor(step/2)%bass.length];beep(n,t,.105,"square",.052);if(step%2===0)beep(n*2,t+.055,.07,"triangle",.025);if(step%4===0)beep(b,t,.18,"square",.038);hat(t);step++},145)}function stopTune(){clearInterval(intervalId);intervalId=undefined;if(masterGain&&audioCtx){masterGain.gain.setTargetAtTime(.0001,audioCtx.currentTime,.04);setTimeout(()=>{try{masterGain.disconnect()}catch{}masterGain=undefined},180)}}if(btn){btn.addEventListener("click",async()=>{if(!playing){if(audioCtx?.state==="suspended")await audioCtx.resume();startTune();playing=true;btn.textContent="■ Stop chiptune";btn.setAttribute("aria-pressed","true")}else{stopTune();playing=false;btn.textContent="▶ Start chiptune";btn.setAttribute("aria-pressed","false")}})}
